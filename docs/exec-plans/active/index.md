# Active Plans

This directory is reserved for in-flight agent-first execution plans (contract / handoff / evaluation triads).

Currently empty — no active plans. Completed plans are not retained; the code is the source of truth once work lands. Historical context for past changes lives in [`../../product/feature-change-log.md`](../../product/feature-change-log.md).
