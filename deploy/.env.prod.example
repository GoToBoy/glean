# Glean Production Environment Configuration
# Copy this file to .env.prod and customize the values
# Usage: docker compose -f docker-compose.prod.yml --env-file .env.prod up -d

# =============================================================================
# DATABASE (REQUIRED - Change these!)
# =============================================================================
POSTGRES_DB=glean
POSTGRES_USER=glean
# Generate a strong password: openssl rand -base64 24
POSTGRES_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD

# =============================================================================
# REDIS (REQUIRED - Change this!)
# =============================================================================
# Generate a strong password: openssl rand -base64 24
REDIS_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD

# =============================================================================
# APPLICATION (REQUIRED - Change these!)
# =============================================================================
# Secret key for JWT token signing
# Generate with: openssl rand -hex 32
SECRET_KEY=CHANGE_ME_USE_LONG_RANDOM_STRING

# Enable debug mode (MUST be false in production)
DEBUG=false

# CORS allowed origins (JSON array format)
# Replace with your actual domain(s)
CORS_ORIGINS=["https://your-domain.com"]

# =============================================================================
# PORTS
# =============================================================================
# Web interface port (behind reverse proxy, usually 80)
WEB_PORT=80

# =============================================================================
# ADMIN (Optional - Full Deployment Only)
# =============================================================================
# Set CREATE_ADMIN=true on first startup to auto-create admin account
# The password will be printed in the logs (save it immediately!)
# CREATE_ADMIN=true
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=
# ADMIN_ROLE=super_admin
